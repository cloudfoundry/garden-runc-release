---
# may want to replace this with your own image
image: docker:///concourse/concourse-ci

platform: linux

inputs:
  - name: garden-runc-release
  - name: garden-runc-version

outputs:
- name: bosh-release

run:
  path: garden-runc-release/ci/scripts/create-release
