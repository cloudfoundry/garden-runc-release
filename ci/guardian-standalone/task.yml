---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: cloudfoundry/garden-runc-release
    tag: ((image_tag))

inputs:
- name: release
- name: iptables-out
  optional: true
- name: tar-out
  optional: true
- name: binary
  optional: true

outputs:
- name: binary

run:
  path: release/ci/guardian-standalone/task
