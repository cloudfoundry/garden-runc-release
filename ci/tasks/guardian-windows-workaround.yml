---
platform: windows

params:
  WORKAROUND_BRANCH: windows-tests-workaround
run:
  path: powershell
  args:
  - "-Command"
  - "&{git clone -b $env:WORKAROUND_BRANCH https://github.com/cloudfoundry/garden-runc-release.git gr-release-develop}"
  - "; powershell"
  - "-ExecutionPolicy"
  - "Bypass"
  - "-File"
  - gr-release-develop/ci/scripts/guardian-windows.ps1
