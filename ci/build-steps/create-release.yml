---
# may want to replace this with your own image
image_resource:
  type: docker-image
  source:
    repository: cfgarden/garden-ci
    tag: '1.1'

platform: linux

inputs:
  - name: gr-release-develop
  - name: gr-version

outputs:
- name: bosh-release

params:
  SKIP_VERSION:

run:
  path: gr-release-develop/ci/build-steps/create-release
